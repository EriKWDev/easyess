import unittest, easyess

comp:
  type
    ObjectComponent = object
      data1: int
      data2: string

createECS()

suite "Components":
  test "Can define object component":
    check defined(ObjectComponent)
    check defined(ckObjectComponent)
